# docker-compose.yml — środowisko dev (hot reload, porty wystawione).
# Wersja produkcyjna: `docker-compose.prod.yml` (Caddy, brak wystawionych
# portów poza 80/443, migracje przed startem API).
#
# Zakres: postgres, redis, api, worker, frontend.

services:
  postgres:
    # pgvector/pgvector:pg16 = oficjalny obraz postgres:16 + prebuildowane
    # rozszerzenie `vector` (ADR-104, Etap AI-1) — `CREATE EXTENSION vector`
    # w migracji `20260906_ai_fund_models.py` wymaga go dostępnego w obrazie,
    # zwykły `postgres:16` go nie ma. Drop-in zamiennik, ta sama wersja majora.
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7
    restart: unless-stopped
    ports:
      - "6379:6379"

  api:
    # Rola aplikacji (`portfel_app`) — bez `BYPASSRLS` i bez własności tabel,
    # więc polityki RLS z migracji `8d1f2a6c40b7` realnie ją obowiązują
    # (ADR-002 warstwa 3, plan krok 44). Hasło nadaje `make db-roles`.
    # `worker` tej zmiennej NIE dostaje: liczy snapshoty wszystkich
    # użytkowników i musi widzieć całą bazę.
    environment:
      DATABASE_URL_APP: ${DATABASE_URL_APP:-postgresql+asyncpg://portfel_app:portfel_app@postgres:5432/portfel}
    build:
      context: ./backend
      dockerfile: Dockerfile
      # Dockerfile jest wielostopniowy (etap 7) — bez `target` zbudowałby
      # się stopień OSTATNI, czyli `prod`, i dev po cichu straciłby hot
      # reload oraz narzędzia z `requirements-dev` (ruff/mypy/pytest,
      # których używa `make check`).
      target: dev
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app

  worker:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: dev
    command: ["python", "-m", "worker.scheduler"]
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: dev
    environment:
      # Osiągalny z PRZEGLĄDARKI (fetch leci z klienta) — `localhost:8000`,
      # nie `http://api:8000` (to działa tylko server-to-server w compose).
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000/api}
    depends_on:
      - api
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      # `.next` NIE może być współdzielone z hostem. `make check` uruchamia
      # `next build` na hoście, a tutaj biegnie `next dev` — obie strony
      # pisały do tego samego katalogu przez bind-mount wyżej i nadpisywały
      # sobie artefakty. Objawy za każdym razem wyglądają jak błąd kodu, a
      # nie środowiska: `ChunkLoadError: Unexpected end of input` z HTTP 500
      # na każdej trasie (2026-08-03) albo — trafione przy pisaniu smoke
      # testu (krok 39) — HTTP 404 na `/portfolios/{id}/struktura`
      # i `/portfolios/{id}/rynki`, przy w pełni działających
      # `/portfolios/{id}` i `/struktura`.
      # Wolumen anonimowy przesłania ten jeden podkatalog: kontener ma własne
      # `.next`, host własne, żadne z nich nie widzi drugiego. Ten sam wzorzec,
      # co `frontend_node_modules` wyżej.
      - /app/.next

volumes:
  postgres_data:
  frontend_node_modules:
