export default function Home() {
  return (
    <section className="flex flex-col gap-4 px-4 py-8 sm:px-6 lg:px-8">
      <h1 className="text-2xl font-semibold tracking-tight">
        Portfel v2 — fundament gotowy
      </h1>
      <p className="max-w-prose text-zinc-600 dark:text-zinc-400">
        Szkielet aplikacji działa. Widoki dashboardu, portfela, struktury i
        rynków powstają w kolejnych etapach — nawigacja poniżej (na mobile) i
        po lewej (na desktopie) prowadzi do przyszłych tras.
      </p>
    </section>
  );
}
