-r requirements.txt

ruff==0.16.0
mypy==2.3.0
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
# Mockuje żądania HTTP klienta Authlib (httpx) do Google w testach OAuth —
# bez tego jedyną alternatywą byłoby monkeypatchowanie wnętrza Authlib albo
# realne wywołania sieciowe (zabronione w testach, docs/konwencje.md).
respx==0.23.1
