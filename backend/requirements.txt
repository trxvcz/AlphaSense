fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
alembic==1.18.5
structlog==26.1.0
redis==8.0.1
pyjwt==2.13.0
argon2-cffi==25.1.0
email-validator==2.3.0
authlib==1.7.2
slowapi==0.1.10
httpx==0.28.1
yfinance==1.5.2
apscheduler==3.11.3
# Newsy (krok 46, etap 9). Parsuje sam XML/Atom — pobieranie zostaje po
# naszej stronie (httpx + `get_with_backoff`), żeby feedy przechodziły przez
# ten sam RateLimiter i CircuitBreaker co reszta dostawców. Powód wyboru
# biblioteki zamiast `xml.etree`: feedy RSS w praktyce łamią specyfikację
# (różne formaty dat, RSS 2.0 obok Atoma, encje HTML w tytułach), a to jest
# dokładnie ta warstwa zgodności, której nie chcemy pisać sami.
feedparser==6.0.11
# Obserwowalność (krok 37). Extra `[fastapi]` dociąga tylko warunek wersji
# FastAPI/Starlette — integracje SDK włączają się same, gdy framework jest
# zainstalowany. Bez `SENTRY_DSN` w środowisku pakiet nic nie robi (dev).
sentry-sdk[fastapi]==2.66.1
